{
  "private": true,
  "scripts": {
    "start": "npm --prefix Backend start",
    "start:backend": "npm --prefix Backend start",
    "dev": "concurrently --kill-others --names backend,frontend \"npm run start:backend\" \"npm --prefix Frontend run dev\"",
    "build": "npm --prefix Frontend run build",
    "lint": "npm --prefix Frontend run lint"
  },
  "dependencies": {
    "react-router-dom": "^7.18.3"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
